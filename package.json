{
  "name": "sivuv-support",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run validate && npm test"
  }
}
